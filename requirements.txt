# PSC-CHIMERA runtime dependencies
# Python >= 3.10
# Fast install: pip install -e .
# Development tools: pip install -r requirements-dev.txt
# Optional molecular-dynamics stack: pip install -e '.[md]'

# Core ML
# Install the platform-appropriate PyTorch build first when using a custom
# CUDA/ROCm wheel index. pip install -e . will otherwise resolve PyTorch.
torch>=2.1.0
transformers>=4.36.0
fair-esm>=2.0.0
einops>=0.7.0

# Bioinformatics
biopython>=1.81

# Structural retrieval
faiss-cpu>=1.7.4

# Data / numerics
numpy>=1.24.0
scipy>=1.11.0
pandas>=2.0.0
h5py>=3.9.0

# Visualization / CLI
matplotlib>=3.7.0
tqdm>=4.65.0
rich>=13.0.0
typer>=0.9.0
